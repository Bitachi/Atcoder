#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define repp(i,a,  n) for (int i = a; i < (int)(n); i++)
typedef pair<int, int> P;

int main(){
  ll n;
  cin >> n;
  cout << ((n-1) * n) / 2 << endl;
}
